# configuration
language: java
cache:
    - apt
    - $HOME/.m2

# build matrix
jdk:
    - oraclejdk8

before_install:
  - sudo apt-get -y install python3-pip python-dev
  - python3 -V
   
# run steps
install:

script:
    - cd textdb 
    - mvn test
