{
	"jdbc": {
		"driver": "com.mysql.cj.jdbc.Driver",
		// default mysql port is 3306, mysql database name is texera_db, make sure to add ?serverTimezone=UTC to avoid issue
		"url": "jdbc:mysql://localhost:3306/texera_db?serverTimezone=UTC",
		// put your database username and password here
		"username": "",
		"password": ""
	}
}
